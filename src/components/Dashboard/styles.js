import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        padding: 5
    },
    empty: {
        display: 'flex',
        alignItems: 'center',
        textAlign: 'center'
    },
    emptyText: {
        textAlign: 'center',
        fontSize: 20,
        fontWeight: 'bold',
        alignSelf: 'center'
    }
});
